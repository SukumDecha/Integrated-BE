spring:
  datasource:
    url: jdbc:mysql://localhost:3308/ecommerce?useSSL=true&serverTimezone=Asia/Bangkok
    username: root
    password: rootpass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true

app:
  cors:
    allowed-origin: http://localhost:5173
